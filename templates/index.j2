{% extends 'base.j2' %}

{% from 'utils.j2' import anchor, plural %}

{% block title %}SCP Wiki Style Statistics{% endblock %}

{% block body %}
  <h1 class="title">Extracted Styles</h1>

  {# ------ #}

  <h2 class="header">
    <a href="/wikidot-css-scraper/module-css.html">
      Module Styles
    </a>
  </h2>
  <p>
    There are {{ module_styles|length }} unique stylings in
    <code>[[module CSS]]</code> blocks across the SCP Wiki.
    The 3 most common are:
  </p>

  <ul>
    {% for style, count in module_styles[:3] %}
      <li>
        <pre><code>{{ style }}</code></pre>
      </li>
    {% endfor %}
  </ul>

  {# ------ #}

  <h2 class="header">
    <a href="/wikidot-css-scraper/inline-css.html">
      Inline Styles
    </a>
  </h2>
  <p>
    There are {{ inline_styles|length }} unique inline stylings
    across the SCP Wiki.
    The 10 most common are:
  </p>

  <ul>
    {% for style, count in inline_styles[:10] %}
      <li>
        <pre><code>{{ style }}</code></pre>
      </li>
    {% endfor %}
  </ul>

  {# ------ #}

  <h2 class="header">
    <a href="/wikidot-css-scraper/includes.html">
      Inclusions
    </a>
  </h2>
  <p>
    There are {{ includes|length }} unique included pages in use across the SCP Wiki.
    The 10 most common are:
  </p>

  <ul>
    {% for include, count in includes[:10] %}
      <li>
        <pre><code>{{ include }}</code></pre>
      </li>
    {% endfor %}
  </ul>

  {# ------ #}

  <h2 class="header">
    <a href="/wikidot-css-scraper/classes.html">
      CSS Classes
    </a>
  </h2>
  <p>
    There are {{ classes|length }} unique CSS classes in use across the SCP Wiki.
    The 10 most common are:
  </p>

  <ul>
    {% for class, count in classes[:10] %}
      <li>
        <pre><code>{{ class }}</code></pre>
      </li>
    {% endfor %}
  </ul>

  {# ------ #}

  <h2 class="header">
    <a href="/wikidot-css-scraper/pages/">
      Pages
    </a>
  </h2>
  <p>
    There are {{ pages|length }} pages on the SCP Wiki.
  </p>

  {# ------ #}

  <h2 class="header">Raw Data</h2>
  <p>
    Raw JSON file:
    <a href="https://github.com/ammongit/wikidot-css-extractor/raw/gh-pages/results.json"><code>results.json</code></a>
    <br>
    This contains all the data used to build this webpage.
  </p>

  <p>
    <strong>Warning!</strong> The file is very large, &gt; 100 MB.
  </p>
{% endblock %}
